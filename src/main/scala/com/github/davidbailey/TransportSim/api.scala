import org.scalatra._
import models.Models._

// calls a bunch of views for the objects in the given bounding box and returns those views as json for openlayers to plot over an openstreetmap using ol.geom.Polygon
